{
  "name": "cert",
  "version": "0.0.0",
  "scripts": {
    "build": "cd src/cert_frontend && npm install && npm run build",
    "dev": "cd src/cert_frontend && npm run dev",
    "start": "cd src/cert_frontend && npm run preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "sass": "^1.83.4",
    "vite": "^4.5.5"
  }
}
